Vanilla HTML/JS only (no frameworks)
API-first (no local catalog/cart storage as system of record)
Demo banner on all pages
Stripe test mode only; store only payment intent id + status
MCP via SalesforceInteractions SDK; always-on features
MCE integration later; opt-in required
